module PostHelper
    # def format_name(post)
    #     post.each do |i|
    #         if i.text=="It is"
    #             Post.find(i.id).update(text:"it")
    #         end
    #     end
    # end
end
